<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">

<!--  该文件用来配置mybatis的数据源和与数据库相关的信息 例如: 用户名，密码 驱动 等信息  -->


<configuration>

    <properties resource="jdbc.properties"/>

    <settings>
        <setting name="lazyLoadingEnabled" value="true" />
        <setting name="aggressiveLazyLoading" value="false" />
    </settings>


    <!--别名-->
    <typeAliases>
        <!--<typeAlias alias="role" type="com.houx.pojo.Role"/>-->
        <package name="com.houx.pojo"/>
    </typeAliases>

    <!--数据库环境-->
    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC"/>
            <dataSource type="POOLED">
                <property name="driver" value="${jdbc.driver}"/>
                <property name="url" value="${jdbc.url}"/>
                <property name="username" value="${jdbc.username}"/>
                <property name="password" value="${jdbc.password}"/>
            </dataSource>
        </environment>
    </environments>


    <!--映射文件-->
    <mappers>
        <!--<mapper resource="com/houx/Mapper/RoleMapper.xml"/>-->
        <!--<mapper resource="com/houx/Mapper/RoleParamsMapper.xml"/>-->
        <!--<mapper resource="com/houx/Mapper/PageParamsMapper.xml"/>-->
        <package name="com.houx.Mapper"/>
    </mappers>

</configuration>